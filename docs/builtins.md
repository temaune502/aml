# Вбудовані модулі

## console
- `print_line(text)` — Виводить рядок з переносом.
- `print_colored(text, color)` — Виводить рядок заданим кольором.

## file
- `read(path)` — Читає файл і повертає вміст.
- `write(path, text)` — Записує текст у файл.