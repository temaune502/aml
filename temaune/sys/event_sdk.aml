// AML Core SDK - Event and Tick management
var registered_tasks = []

func start_periodic_task(callback, rate=1) {
    print("Registering periodic task with rate:", rate)
    tick(callback, rate)
    registered_tasks.append(@callback)
}

func monitor_runtime() {
    // spec.tick is updated 20 times per second (1-20)
    if (spec.tick == 1) {
        // print("New second started in runtime")
    }
}

// Start monitoring by default
tick(@monitor_runtime, 20)
