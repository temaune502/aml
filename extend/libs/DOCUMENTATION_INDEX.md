# Індекс документації проєкту `libs`

Повна документація проєкту для автоматизації керування мишею, клавіатурою та роботи з кривими.

## 📄 Основна документація

### [`README.md`](README.md)
**Головна документація проєкту** — детальний опис всіх модулів та функцій.

Містить:
- Структура проєкту
- Повний опис модуля `automation.py` з усіма функціями
- Повний опис модуля `curves.py` з усіма функціями
- Приклади використання для всіх типів рухів миші
- Рекомендації для найкращих результатів
- Вказівки щодо безпеки

---

## 📚 Документація модулів

### `plugins/automation.md`
Документація модуля `automation.py` українською.

**Розділи:**
- Функції управління мишею (базові операції, кліки, перетягування, прокрутка)
- Функції управління клавіатурою (натискання, комбінації, введення тексту)
- Очікування клавіші
- Просунуті рухи миші (синусоїда, спіраль, коло, зигзаг, та ін.)
- Допоміжні функції

**Цільова аудиторія:** розробники, які хочуть використовувати функції управління мишею та клавіатурою.

### `plugins/curves.md`
Документація модуля `curves.py` українською.

**Розділи:**
- Функції easing (згладжування)
- Інтерполяція точок (Bezier, Catmull-Rom)
- Генерація траєкторій (синусоїда, спіраль, коло, зигзаг, випадковий маршрут, шум)
- Аналіз траєкторій
- Примітки та рекомендації

**Цільова аудиторія:** розробники, які працюють з математичними кривими та траєкторіями.

---

## 📋 Приклади використання

### `examples_automation.py`
**17 практичних прикладів** використання модуля `automation`.

**Приклади:**
1. Базові операції з мишею
2. Кліки та натискання кнопок миші
3. Перетягування
4. Прокрутка
5. Керування клавіатурою
6. Введення тексту
7. Очікування клавіші
8. Рухи миші по синусоїді
9. Рухи миші по спіралі
10. Рухи миші по колу та дугам
11. Зигзагоподібні рухи
12. Випадковий маршрут
13. Рух з гаусівським шумом (реалістичний)
14. Рух через вузлові точки (інтерполяція)
15. Комбінований рух (первинна крива + шум)
16. Практичний сценарій: автоклік по елементам
17. Практичний сценарій: заповнення форми

**Запуск:** `python examples_automation.py`

### `examples_curves.py`
**12 практичних прикладів** використання модуля `curves`.

**Приклади:**
1. Синусоїдальна траєкторія
2. Спіральна траєкторія
3. Коло та дуги
4. Зигзаг
5. Гаусівський шум (реалістичний рух)
6. Випадковий маршрут
7. Інтерполяція через вузлові точки
8. Комбінований рух (первинна крива + вторинний шум)
9. Easing функції
10. Аналіз траєкторій
11. Практичний сценарій: рисування фігур
12. Практичний сценарій: автозаповнення форми

**Запуск:** `python examples_curves.py`

---

## 🎯 Швидкий старт

### Встановлення залежностей
```bash
pip install pynput
```

### Базовий приклад з automation

```python
from plugins import automation

# Отримати розмір екрану
size = automation.get_screen_size()
print(f"Екран: {size}")

# Переміщення миші до центру
center = (size[0] // 2, size[1] // 2)
automation.move_mouse_smooth(center[0], center[1], duration=0.5)

# Клік
automation.click()

# Введення тексту
automation.type_text_human('Hello!')

# Гарячі клавіші
automation.hotkey('ctrl', 's')
```

### Базовий приклад з curves

```python
from plugins import curves

# Генерування синусоїдальної траєкторії
path = curves.sine_wave((100, 100), (600, 400), amplitude=50, frequency=2, steps=100)

# Генерування спіралі
spiral = curves.spiral_path((400, 300), start_radius=20, end_radius=150, turns=2, steps=100)

# Інтерполяція через вузлові точки
waypoints = [(100, 100), (300, 200), (500, 100)]
smooth_path = curves.interpolate_path(waypoints, curve_type='catmull', steps_per_segment=20)

# Аналіз траєкторії
length = curves.path_length(smooth_path)
print(f"Довжина шляху: {length:.2f} пікселів")
```

### Комплексний приклад: рух миші по реалістичній траєкторії

```python
from plugins import automation

# Синусоїда з шумом для реалістичності
automation.move_mouse_composite(
    start=(100, 100),
    end=(700, 400),
    pattern='sine',
    secondary_noise=10,
    duration=2.0
)

# Або можна використовувати інтерполяцію
waypoints = [(100, 100), (250, 300), (400, 150), (600, 400)]
automation.move_mouse_interpolated(
    waypoints,
    steps_per_segment=20,
    curve_type='catmull',
    duration=2.0
)
```

---

## 🔍 Пошук функцій за типом

### Рухи миші
- **Плавні/базові:** `move_mouse_smooth()`, `move_mouse_relative_smooth()`
- **Синусоїда:** `move_mouse_sine()`
- **Спіраль:** `move_mouse_spiral()`
- **Коло:** `move_mouse_circle()`
- **Зигзаг:** `move_mouse_zigzag()`
- **Випадковий:** `move_mouse_random_walk()`
- **З шумом:** `move_mouse_noisy()`
- **Через вузлові точки:** `move_mouse_interpolated()`
- **Комбінований:** `move_mouse_composite()`

### Кліки та натискання
- **Простий клік:** `click()`
- **Подвійний клік:** `double_click()`
- **Правий клік:** `right_click()`
- **Утримування кнопки:** `mouse_down()`, `mouse_up()`

### Клавіатура
- **Натискання клавіші:** `press_key()`
- **Комбінація:** `hotkey()`
- **Послідовність комбінацій:** `hotkey_sequence()`
- **Введення тексту:** `type_text()`, `type_text_human()`
- **Очікування:** `wait_for_key()`

### Криві та траєкторії (модуль curves)
- **Easing:** `ease_in_*()`, `ease_out_*()`
- **Інтерполяція:** `lerp()`, `quadratic_bezier()`, `cubic_bezier()`, `catmull_rom()`
- **Генерація:** `sine_wave()`, `spiral_path()`, `circle_path()`, `zigzag_path()`, `random_walk_path()`, `gaussian_noise_path()`, `composite_path()`
- **Аналіз:** `path_length()`, `path_velocity()`, `resample_path()`

---

## 📝 Порядок читання документації

**Новичкам:**
1. Прочитати [README.md](README.md) — загальний огляд
2. Розглянути приклади у `examples_automation.py`
3. Експериментувати з функціями з `automation.py`

**Для роботи з кривими:**
1. Прочитати [`plugins/curves.md`](plugins/curves.md)
2. Запустити `examples_curves.py`
3. Прочитати детальні приклади в [`README.md`](README.md)

**Просунутим користувачам:**
1. Комбінувати функції з обох модулів
2. Читати докладні приклади в розділі "Приклади" у [`README.md`](README.md)
3. Вивчити вихідний код `plugins/curves.py` для розуміння математики

---

## 🔧 Технічні деталі

### Залежності
- `pynput` — керування мишею та клавіатурою

### Сумісність
- Windows: повна підтримка (включно з WinAPI для розміру екрану)
- Linux/macOS: базова підтримка pynput

### Версія Python
- Python 3.7+

---

## 💡 Поради

1. **Для реалістичного руху** використовуйте `move_mouse_composite()` з `secondary_noise`
2. **Для малювання** використовуйте `move_mouse_interpolated()` з `curve_type='catmull'`
3. **Для анімацій** комбінуйте `move_mouse_sine()`, `move_mouse_spiral()`, `move_mouse_circle()`
4. **Для тестування** використовуйте малі затримки та координати для безпеки
5. **Завжди зберігайте можливість зупинити скрипт** (Ctrl+C)

---

## 📞 Контроль якості

- ✅ Всі файли мають синтаксичні перевірки (py_compile)
- ✅ Модулі успішно імпортуються
- ✅ Приклади готові до запуску
- ✅ Вся документація українською

---

**Остання оновлення:** 16 листопада 2025 р.
